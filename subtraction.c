# include<stdio.h>
int main()
{
    int a, b,sub ;
    printf("Enter the values of a , b:");
    scanf("%d%d",&a,&b);
    sub=a-b;
    printf("subtraction of the two value is: %d",sub );
    return 0;
}