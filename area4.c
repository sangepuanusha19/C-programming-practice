# include<stdio.h>
int main()
{
    float area,length,breadth;
    printf("enter the values of length and breadt");
    scanf("%f%f",&length,&breadth);
    area=length*breadth;
    printf("area of the rectangle:%f",area);
    return 0;
}