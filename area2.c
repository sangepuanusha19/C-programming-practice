# include<stdio.h>
int main()
{
   float area,length,height; 
   printf("enter the value of length,height");
   scanf("%f%f",&length,&height);
   area of the triangle=0.5*length*height;
   printf("area of the triangle:%f",area);
   return 0;
}